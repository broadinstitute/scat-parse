# Nav order: the index page first, then the additive pages. Without this Laika sorts by title and
# "Generalizing over input" lands above "cats-parse".
laika.navigationOrder = [
  index.md
  generalizing-over-input.md
]
